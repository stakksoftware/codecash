{
  "name": "@codecash/sdk",
  "version": "0.2.0",
  "description": "CodeCash latency-monetization SDK (Phase 4). Opt-in wrapper that lets a third-party app monetize its own wait-states / time-to-first-token, on the same trust-first, privacy-first terms as the CLI. Built on @codecash/core.",
  "license": "MIT",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "exports": {
    ".": "./index.js"
  },
  "dependencies": {
    "@codecash/core": "0.2.0"
  },
  "scripts": {
    "test": "node --test test/*.test.js"
  }
}
